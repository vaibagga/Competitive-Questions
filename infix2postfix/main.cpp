#include <iostream>
#include <stack>

using namespace std;

int precedence (char op){


}

bool isOperator (char ch){
    if (ch == '+' || ch == '-' || ch == '/' || ch == '*' || ch == '^')
        return true;
    return false;
}

infix2postfix(string p){
    string infix = "";
    for (int i = 0; i < p.length(); i++){
        if ()
    }
}

int main()
{
    cout << "Hello world!" << endl;
    return 0;
}
